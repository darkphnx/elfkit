%p Hi #{@participant.name},

%p Just a quick reminder to let you know we'll be matching gifter and giftees for <b>#{@exchange.title}</b> in just under a week.

%p <b>If you can no longer participate</b>, please click the link below and press the "Leave the Exhcange!" button.

%p
  - link = exchange_participant_retrieve_url(@exchange, @participant, @participant.login_token, :host => Elfkit::Application.config.protocol, :domain => Elfkit::Application.config.domain)
  = link_to(link, link, style: "color: #c80008;")

%p We'll be matching gifter and giftees on <b>#{@exchange.match_at.to_s(:long)}</b>.
