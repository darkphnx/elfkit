- title @exchange.title

.row
  .twelve.columns
    %h1= @exchange.title

.row
  .six.columns
    %h4 Share this URL with your friends and colleagues:

  .six.columns
    %h2= link_to "#{request.domain}#{exchange_path(@exchange)}", exchange_url(@exchange)

%hr

= render @exchange.stage, exchange: @exchange

%hr

.row
  .six.columns
    %h2 Participants:
  .six.columns
    %ul
      - @participants.each do |participant|
        %li= participant.name

      - if @participants.empty?
        %li No confirmed participants yet, be the first!
